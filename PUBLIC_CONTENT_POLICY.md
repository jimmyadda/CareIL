# CareIL bilingual public-content policy

- Add every new public article in both Hebrew and English with the same slug.
- Hebrew content lives in `package/content_he.py`; English content lives in `package/content_en.py`.
- Give each language version natural, reviewed copy plus its own title and description.
- Keep canonical and `hreflang` links aligned and include both languages in the sitemap.
